# Горбаненко Кирилл ИТ-3 Лабораторная №1

# Задание 1
## Задача 1
### Текст задачи
Букву в число.
Дана сигнатура метода: `public int charToNum (char x);`
Метод принимает символ x, который представляет собой один из "0123456789". Необходимо реализовать метод таким образом, чтобы он преобразовывал символ в соответствующее число. Подсказка: код символа '0' это число 48.
Пример: x = '3', результат: 3

### Алгоритм решения
1. Символ '0' имеет ASCII код 48. Все цифры '0'-'9' идут в таблице ASCII последовательно.
2. Чтобы получить числовое значение символа `x`, необходимо вычесть код символа '0' из `x`: `x - '0'`.
3. Метод возвращает полученный целочисленный результат.

## Задача 2
### Текст задачи
Есть ли позитив.
Дана сигнатура метода: `public boolean isPositive (int x);`
Необходимо реализовать метод таким образом, чтобы он принимал число x и возвращал true, если оно положительное.
Пример 1: x = 3, результат: true
Пример 2: x = -5, результат: false

### Алгоритм решения
1. Проверить значение `x` на строгое неравенство `x > 0`.
2. Если `x` больше 0, вернуть `true`, в противном случае (если `x <= 0`) вернуть `false`.

## Задача 3
### Текст задачи
Большая буква.
Дана сигнатура метода: `public boolean isUpperCase (char x);`
Необходимо реализовать метод таким образом, чтобы он принимал символ x и возвращал true, если это большая буква в диапазоне от 'A' до 'Z'.
Пример 1: x = 'D', результат: true
Пример 2: x = 'q', результат: false

### Алгоритм решения
1. Проверить, находится ли символ `x` в диапазоне заглавных латинских букв: `x >= 'A' && x <= 'Z'`.
2. Вернуть результат логического выражения.

## Задача 4
### Текст задачи
Делитель.
Дана сигнатура метода: `public boolean isDivisor (int a, int b);`
Необходимо реализовать метод таким образом, чтобы он возвращал true, если любое из принятых чисел делит другое нацело.
Пример 1: a = 3, b = 6, результат: true
Пример 2: a = 2, b = 15, результат: false

### Алгоритм решения
1. Проверить, делится ли `a` на `b` без остатка (`b != 0 && a % b == 0`) или делится ли `b` на `a` без остатка (`a != 0 && b % a == 0`).
2. Вернуть `true`, если хотя бы одно из условий выполняется, иначе `false`.

## Задача 5
### Текст задачи
Многократный вызов.
Дана сигнатура метода: `public int lastNumSum(int a, int b)`
Необходимо реализовать метод таким образом, чтобы он считал сумму цифр двух чисел из разряда единиц. Выполните с его помощью последовательное сложение пяти чисел и результат выведите на экран. Постарайтесь выполнить задачу, используя минимально возможное количество вспомогательных переменных.
Пример:
5 + 11 это 6
6 + 123 это 9
9 + 14 это 13
13 + 1 это 4
Итого 4

### Алгоритм решения
1. В методе `lastNumSum(int a, int b)` выделить последнюю цифру (разряд единиц) каждого из чисел через операцию остатка от деления на 10: `Math.abs(a % 10) + Math.abs(b % 10)`.
2. В основном методе производить цепочку вызовов `lastNumSum`, последовательно передавая предыдущий результат и следующее число из набора 5 чисел.
3. Вывести итоговый результат на экран.

# Задание 2
## Задача 1
### Текст задачи
Модуль числа.
Дана сигнатура метода: `public int abs (int x);`
Необходимо реализовать метод таким образом, чтобы он возвращал модуль числа x (если оно было положительным, то таким и остается, если оно было отрицательным, то необходимо вернуть его без знака минус).
Пример 1: x = 5, результат: 5
Пример 2: x = -3, результат: 3

### Алгоритм решения
1. Проверить условие `x < 0`.
2. Если число отрицательное, вернуть `-x`.
3. В противном случае вернуть `x`.

## Задача 2
### Текст задачи
Строка сравнения.
Дана сигнатура метода: `public String makeDecision (int x, int y);`
Необходимо реализовать метод таким образом, чтобы он возвращал строку, которая включает два принятых методом числа и корректно выставленный знак операции сравнения (больше, меньше, или равно).
Пример 1: x = 5, y = 7, результат: "5 < 7"
Пример 2: x = 8, y = -1, результат: "8 > -1"
Пример 3: x = 4, y = 4, результат: "4 == 4"

### Алгоритм решения
1. Сравнить `x` и `y`.
2. Если `x > y`, сформировать и вернуть строку `x + " > " + y`.
3. Если `x < y`, сформировать и вернуть строку `x + " < " + y`.
4. В противном случае вернуть строку `x + " == " + y`.

## Задача 3
### Текст задачи
Тройной максимум.
Дана сигнатура метода: `public int max3 (int x, int y, int z);`
Необходимо реализовать метод таким образом, чтобы он возвращал максимальное из трех полученных методом чисел. Подсказка: идеальное решение включает всего две инструкции if и не содержит вложенных if.
Пример 1: x = 5, y = 7, z = 7, результат: 7
Пример 2: x = 8, y = -1, z = 4, результат: 8

### Алгоритм решения
1. Инициализировать переменную `max = x`.
2. Если `y > max`, присвоить `max = y`.
3. Если `z > max`, присвоить `max = z`.
4. Вернуть значение `max`.

## Задача 4
### Текст задачи
Двойная сумма.
Дана сигнатура метода: `public int sum2 (int x, int y);`
Необходимо реализовать метод таким образом, чтобы он возвращал сумму чисел x и y. Однако, если сумма попадает в диапазон от 10 до 19, то надо вернуть число 20.
Пример 1: x = 5, y = 7, результат: 20
Пример 2: x = 8, y = -1, результат: 7

### Алгоритм решения
1. Вычислить сумму `int sum = x + y`.
2. Проверить, находится ли сумма в отрезке `[10, 19]`: `if (sum >= 10 && sum <= 19)`.
3. Если да, вернуть `20`, иначе вернуть значение `sum`.

## Задача 5
### Текст задачи
День недели.
Дана сигнатура метода: `public String day (int x);`
Метод принимает число x, обозначающее день недели. Необходимо реализовать метод таким образом, чтобы он возвращал строку, которая будет обозначать текущий день недели, где 1 - это понедельник, а 7 - воскресенье. Если число не от 1 до 7, то верните текст "это не день недели". Вместо if в данной задаче используйте switch.
Пример: x = 5, результат: "пятница"

### Алгоритм решения
1. Использовать инструкцию `switch (x)`.
2. Для значений от 1 до 7 вернуть названия дней недели со строчной буквы ("понедельник", "вторник", ..., "воскресенье").
3. Для блока `default` вернуть строку "это не день недели".

# Задание 3
## Задача 1
### Текст задачи
Числа наоборот.
Дана сигнатура метода: `public String reverseListNums (int x);`
Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой будут записаны все числа от x до 0 (включительно).
Пример: x = 5, результат: "5 4 3 2 1 0"

### Алгоритм решения
1. Создать объект `StringBuilder` для формирования итоговой строки.
2. Организовать цикл `for (int i = x; i >= 0; i--)`.
3. Добавлять текущее число `i` и пробел к `StringBuilder`.
4. Вернуть полученную строку (применив `.trim()`).

## Задача 2
### Текст задачи
Четные числа.
Дана сигнатура метода: `public String chet (int x);`
Необходимо реализовать метод таким образом, чтобы он возвращал строку, в которой будут записаны все четные числа от 0 до x (включительно). Подсказка для обеспечения качества кода: инструкцию if использовать не следует.
Пример: x = 9, результат: "02468"

### Алгоритм решения
1. Создать объект `StringBuilder` для формирования итоговой строки.
2. Организовать цикл `for (int i = 0; i <= x; i += 2)`.
3. В каждой итерации добавлять число `i` к `StringBuilder`.
4. Преобразовать `StringBuilder` в строку и вернуть её.

## Задача 3
### Текст задачи
Одинаковость.
Дана сигнатура метода: `public boolean equalNum (int x);`
Необходимо реализовать метод таким образом, чтобы он возвращал true, если все знаки числа одинаковы, и false в ином случае.
Пример 1: x = 1111, результат: true
Пример 2: x = 1211, результат: false

### Алгоритм решения
1. Взять модуль числа `x = Math.abs(x)`.
2. Сохранить последнюю цифру числа: `int lastDigit = x % 10`.
3. Пока `x > 0`, сравнивать текущую последнюю цифру `x % 10` с `lastDigit`.
4. Если цифры не совпадают, вернуть `false`. Разделить `x` на 10.
5. Если цикл завершился без расхождений, вернуть `true`.

## Задача 4
### Текст задачи
Левый треугольник.
Дана сигнатура метода: `public void leftTriangle (int x);`
Необходимо реализовать метод таким образом, чтобы он выводил на экран треугольник из символов '*', у которого x символов в высоту, а количество символов в ряду совпадает с номером строки.
Пример: x = 4
Результат:
*
**
***
****

### Алгоритм решения
1. Внешним циклом `for (int i = 1; i <= x; i++)` перебирать номер строки от 1 до `x`.
2. Внутренним циклом `for (int j = 1; j <= i; j++)` выводить символ `*` без перехода на новую строку.
3. После внутреннего цикла производить переход на новую строку `System.out.println()`.

## Задача 5
### Текст задачи
Правый треугольник.
Дана сигнатура метода: `public void rightTriangle (int x);`
Необходимо реализовать метод таким образом, чтобы он выводил на экран треугольник из символов '*', у которого x символов в высоту, а сами символы выровнены по правому краю.
Пример: x = 4
Результат:
   *
  **
 ***
****

### Алгоритм решения
1. Внешний цикл `for (int i = 1; i <= x; i++)` регулирует номер строки.
2. Внутренний цикл выводит `x - i` пробелов для выравнивания по правому краю.
3. Следующий внутренний цикл выводит `i` символов `*`.
4. Переход на новую строку после завершения вывода текущей строки.

# Задание 4
## Задача 1
### Текст задачи
Поиск первого значения.
Дана сигнатура метода: `public int findFirst (int[] arr, int x);`
Необходимо реализовать метод таким образом, чтобы он возвращал индекс первого вхождения числа x в массив arr. Если число не входит в массив, возвращается -1.
Пример: arr = [1, 2, 3, 4, 2, 2, 5], x = 2, результат: 1

### Алгоритм решения
1. Организовать цикл `for` с индексом `i` от 0 до `arr.length - 1`.
2. Если `arr[i] == x`, незамедлительно вернуть текущий индекс `i`.
3. Если цикл завершился и число не найдено, вернуть `-1`.

## Задача 2
### Текст задачи
Поиск последнего значения.
Дана сигнатура метода: `public int findLast (int[] arr, int x);`
Необходимо реализовать метод таким образом, чтобы он возвращал индекс последнего вхождения числа x в массив arr. Если число не входит в массив, возвращается -1.
Пример: arr = [1, 2, 3, 4, 2, 2, 5], x = 2, результат: 5

### Алгоритм решения
1. Организовать цикл `for` с индексом `i` от `arr.length - 1` до 0 в обратном порядке.
2. При первом совпадении `arr[i] == x` вернуть индекс `i`.
3. Если совпадений нет, вернуть `-1`.

## Задача 3
### Текст задачи
Посчитать позитив.
Дана сигнатура метода: `public int countPositive (int[] arr);`
Необходимо реализовать метод таким образом, чтобы он возвращал количество положительных элементов массива arr.
Пример: arr = [1, -2, 3, 0, -4, 5], результат: 3

### Алгоритм решения
1. Инициализировать счетчик `int count = 0`.
2. Пройти по всем элементам массива `arr`.
3. Если элемент строго больше 0 (`num > 0`), увеличить `count` на 1.
4. Вернуть значение `count`.

## Задача 4
### Текст задачи
Добавление массива в массив.
Дана сигнатура метода: `public int[] add (int[] arr, int[] ins, int pos);`
Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, в котором элементы массива `ins` вставлены в массив `arr`, начиная с позиции `pos`.
Пример: arr = [1, 2, 3, 4, 5], ins = [7, 8, 9], pos = 2, результат: [1, 2, 7, 8, 9, 3, 4, 5]

### Алгоритм решения
1. Создать новый массив `result` размером `arr.length + ins.length`.
2. Скопировать первые `pos` элементов из `arr` в `result`.
3. Скопировать все элементы массива `ins` в `result`, начиная с индекса `pos`.
4. Скопировать оставшиеся элементы из `arr` (начиная с `pos`) в `result` после массива `ins`.
5. Вернуть `result`.

## Задача 5
### Текст задачи
Удалить негатив.
Дана сигнатура метода: `public int[] deleteNegative (int[] arr);`
Необходимо реализовать метод таким образом, чтобы он возвращал новый массив, из которого удалены все отрицательные элементы исходного массива arr.
Пример: arr = [1, -2, 3, 0, -4, 5], результат: [1, 3, 0, 5]

### Алгоритм решения
1. Подсчитать количество неотрицательных элементов (`val >= 0`) в исходном массиве `arr`.
2. Создать новый массив `result` подсчитанного размера.
3. Пройти по `arr` и записать все элементы `>= 0` в `result`.
4. Вернуть `result`.
